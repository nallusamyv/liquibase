import boto3
import json

def lambda_handler(event, context):
    # Extract the bucket name and object key from the event data
    bucket = event['Records'][0]['s3']['bucket']['name']
    key = event['Records'][0]['s3']['object']['key']
    
    # Now you have the bucket and key, you can perform operations on the object
    print(f"Object modified in bucket '{bucket}' with key '{key}'")

    # Add your logic to process the modified object here
